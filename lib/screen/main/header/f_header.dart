import 'package:flutter/material.dart';

class HeaderFrame extends StatelessWidget {
  const HeaderFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
